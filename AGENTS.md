# Desktop Clock 项目说明

## 项目目标

这是一个供初学者学习 Python 和 Codex 工作流的简单桌面时钟程序。

## 技术要求

- 使用 Python 3
- GUI 使用 Python 标准库 Tkinter
- 可以引入第三方依赖
- 程序入口使用 `main.py`
- 代码应保持简单、清晰，方便初学者阅读

## 功能要求

- 显示电脑本机当前时间
- 默认时间格式为 `HH:MM:SS`
- 默认显示秒，用户可以通过设置关闭秒显示
- 时间应实时更新
- 每次显示的时间都应从系统当前时间获取，不要通过内部计数器自行累加时间
- 支持时钟、倒计时、秒表和闹钟四种模式

## 编码规则

- 不要为了简单功能进行过度设计
- 不要创建没有必要的文件和抽象层
- GUI 主线程中禁止使用 `time.sleep()` 阻塞界面
- 优先使用 Tkinter 的 `after()` 实现定时更新
- 倒计时和秒表应基于 `time.monotonic()` 计算真实经过时间
- 系统时钟继续使用 `datetime.now()` 获取本机当前时间
- 函数和变量使用容易理解的名字

## 第三方依赖

- GUI 使用 Python 标准库 Tkinter
- 图片加载、缩放、裁剪等图片处理允许使用 Pillow
- 除 Pillow 外，不要未经明确要求添加其它第三方依赖
- 如果新增第三方依赖，必须同步更新 requirements.txt

## 项目数据

- 应用默认用户数据目录为 Windows `%LOCALAPPDATA%\DesktopClock`
- 不允许在发布代码中写死开发者机器的盘符、用户名或绝对路径
- 可以通过 `DESKTOP_CLOCK_DATA_DIR` 环境变量覆盖用户数据目录
- 发布版必须能够在没有 `D:` 盘的 Windows 电脑运行
- 新增配置、缓存、日志、程序复制或生成的资源等功能时，应基于统一的应用数据目录获取路径
- 只在实际需要时创建文件或子目录，不要提前创建空目录

## 完成任务后

修改代码后：

1. 检查 Python 语法是否正确
2. 如果环境允许，运行程序验证
3. 告诉我修改了哪些文件
4. 简要说明实现方式
